{
  "name": "medelite-facility-snapshot",
  "version": "1.0.0",
  "private": true,
  "description": "INFINITE — Managed by MEDELITE | Facility Assessment Snapshot report generator (CMS Care Compare → branded PDF/Word exports).",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --workspace server run dev",
    "dev:client": "npm --workspace client run dev",
    "build": "npm --workspace server run build && npm --workspace client run build",
    "start": "npm --workspace server run start",
    "lint": "npm --workspace server run lint && npm --workspace client run lint",
    "test": "npm --workspace server run test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
